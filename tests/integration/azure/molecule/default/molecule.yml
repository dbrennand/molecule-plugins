---
dependency:
  name: galaxy
driver:
  name: azure
platforms:
  - name: instance
provisioner:
  name: ansible
  config_options:
    defaults:
      callback_whitelist: profile_roles,profile_tasks,timer
  playbooks:
    create: create.yml
    destroy: destroy.yml
verifier:
  name: testinfra
